<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Show Products</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            sans: ['Inter', 'sans-serif'],
          },
          colors: {
            dark: '#0f172a',
            lightDark: '#1e293b',
            accent: '#3b82f6'
          }
        }
      }
    };
  </script>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
</head>
<body class="bg-gradient-to-br from-gray-900 via-gray-800 to-gray-900 min-h-screen text-white font-sans">

  <div class="max-w-7xl mx-auto px-4 py-10">
    <h1 class="text-4xl font-bold text-center mb-10 text-white">All Products</h1>

    <div class="grid gap-8 sm:grid-cols-2 lg:grid-cols-3">
      
      <% products.forEach(product => { %>
      
        <div class="bg-white/5 backdrop-blur-sm border border-white/10 rounded-xl overflow-hidden shadow-lg hover:scale-[1.02] transition transform duration-200">

          
          <div class="p-5 space-y-2">
            <h2 class="text-xl font-semibold text-white"><%= product._id %></h2>
            <h2 class="text-xl font-semibold text-white"><%= product.name %></h2>
            <p class="text-gray-300">Price: <span class="text-white font-medium"><%= product.price %></span></p>
            <p class="text-gray-300">Category: <span class="text-white font-medium"><%= product.category %></span></p>
            <p class="text-gray-300">Quantity: <span class="text-white font-medium"><%= product.quantity %></span></p>
          </div>

          
          <div class="px-5 pb-5 pt-3 flex justify-end space-x-3 border-t border-white/10">
      
            <a href="/updateproduct/<%= product._id %>" title="Edit" class="text-accent hover:text-blue-400 transition">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 inline-block" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                  d="M11 4h2a2 2 0 012 2v2m4 0l-6 6m0 0L7 20H4v-3l6-6m6-6l4 4" />
              </svg>
            </a>

        
            <a href="/adminproduct/<%= product._id %>" title="Delete" onclick="return confirm('Are you sure you want to delete this product?');" class="text-red-500 hover:text-red-400 transition">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 inline-block" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                  d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6M9 7V4a1 1 0 011-1h4a1 1 0 011 1v3m-7 0h8" />
              </svg>
            </a>
          </div>

        </div>
      <% }) %>

    </div>
  </div>

</body>
</html>
